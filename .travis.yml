language: python
python:
  - 2.7
notifications:
  email: false

# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: 
  - cd operations/ekos/test
  - py.test
